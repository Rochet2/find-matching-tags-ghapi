Drop nick-fields/assert-action and add moving major-tag workflow

test.yml
- Replace every nick-fields/assert-action step with a dep-free shell
  assertion that routes the actual/expected values through env: vars
  (avoids GitHub-expression script injection and removes the third-party
  action dependency)
- Use jq (preinstalled on ubuntu-latest) to print the first element of
  the asc/desc tag arrays via env vars as well

release.yml
- New workflow: when a vX.Y.Z GitHub Release is published, move the
  v<MAJOR> tag to point at the same commit so consumers pinned to
  @v1 automatically pick up patch/minor releases. Skips silently if
  the release tag is not in semver form.

README.md
- Update the assertion example to the new dep-free pattern
- Add a Versioning section documenting the @v1 moving-tag convention
